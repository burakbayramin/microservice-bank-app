server:
  port: 9000
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

build:
  version: "1.2"

cards:
  message: "Welcome to Bank cards related local APIs "
  contactDetails:
    name: "ABC - Developer"
    email: "abc@eazybank.com"
  onCallSupport:
    - (412) 419-3491
    - (915) 382-1932
